services:
  web:
    build:
      context: .
      dockerfile: docker/Dockerfile.dev
    ports:
      - "5050:5050"
    env_file:
      - .flaskenv
    volumes:
      - .:/app
      - /app/.venv
    networks:
      - local-network

networks:
  local-network:
    driver: bridge
