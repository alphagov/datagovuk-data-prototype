{
    "title": "Page title shown in browser tab and as h1",
    "source": {
        "documentation-url": "URL of the page the data was published on",
        "data-url": "URL of the original data file (csv, xlsx, etc.)",
        "notes": "What we extracted, selected, pivoted etc. from the source"
    },
    "data": {
        "csv": "your-data-file.csv",
        "reverse": false,
        "x_col": "X column name (must match CSV header exactly)",
        "x_type": "int",
        "y_cols": [
            {"col": "Y column name (must match CSV header exactly)", "type": "float"}
        ]
    },
    "chart": {
        "title": {"text": "Chart title shown above the plot"},
        "xAxis": {"title": {"text": "X axis label"}},
        "yAxis": {"title": {"text": "Y axis label"}},
        "legend": {"enabled": false},
        "series": [{"name": "Series name shown in tooltip"}]
    }
}
